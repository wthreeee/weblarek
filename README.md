# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```
## Сборка

```
npm run build
```

или

```
yarn build
```
# Интернет-магазин «Web-Larёk»
«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

## Презентер

Презентер реализован в `src/main.ts` и отвечает за всю логику страницы интернет-магазина. Он не генерирует события — только подписывается на них и выполняет действия:
- подписывается на события из моделей и представлений;
- вызывает методы моделей для сохранения данных;
- перерисовывает представления только при событиях модели или при открытии модального окна;
- выбирает, какой компонент показать в модальном окне (корзину, карточку товара, форму заказа, форму контактов, экран успешного заказа).

Выбранный подход: презентер реализован как центральный контроллер приложения в `main.ts` с инверсией зависимостей. В модели данных и в представлениях используется общий брокер событий `EventEmitter`, а презентер работает с model/view через абстрактные интерфейсы и события. Это позволяет снизить связанность и сохранить MVP-подход.

## Данные

#### Класс Component
Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`


#### Класс Api
Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter
Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

## Данные

В приложении используются две основные сущности данных: товар и покупатель. Они описаны следующими интерфейсами:

### Интерфейс товара (IProduct)
Описывает структуру данных для товара в каталоге.

```typescript
interface IProduct {
  id: string;          // Уникальный идентификатор товара
  description: string; // Описание товара
  image: string;       // URL изображения товара
  title: string;       // Название товара
  category: string;    // Категория товара
  price: number | null; // Цена товара (может быть null, если цена не указана)
}
```

### Интерфейс покупателя (IBuyer)
Описывает структуру данных для информации о покупателе при оформлении заказа.

```typescript
interface IBuyer {
  payment: TPayment;   // Вид оплаты (тип TPayment не указан, предполагается enum или string)
  email: string;       // Email покупателя
  phone: string;       // Телефон покупателя
  address: string;     // Адрес доставки
}
```

## Модели данных

Модели данных отвечают за хранение и работу с данными в приложении. Они разделены по зонам ответственности: каталог товаров, корзина и данные покупателя.

### Класс ProductsCatalog
Отвечает за хранение и управление каталогом товаров, включая список всех товаров и товар для подробного просмотра.

**Конструктор:**  
Не принимает параметров.

**Поля класса:**  
`products: IProduct[]` - массив всех товаров в каталоге.  
`preview: IProduct | null` - товар, выбранный для подробного отображения (null, если не выбран).

**Методы класса:**  
`setProducts(products: IProduct[]): void` - сохраняет массив товаров, полученный в параметрах.  
`getProducts(): IProduct[]` - возвращает массив всех товаров.  
`getProductById(id: string): IProduct | undefined` - возвращает товар по его id или undefined, если товар не найден.  
`setPreview(product: IProduct): void` - сохраняет товар для подробного отображения.  
`getPreview(): IProduct | null` - возвращает товар для подробного отображения.

### Класс Basket
Отвечает за хранение и управление товарами, выбранными покупателем для покупки.

**Конструктор:**  
Не принимает параметров.

**Поля класса:**  
`items: IProduct[]` - массив товаров в корзине.

**Методы класса:**  
`getItems(): IProduct[]` - возвращает массив товаров в корзине.  
`addItem(item: IProduct): void` - добавляет товар в корзину.  
`removeItem(item: IProduct): void` - удаляет товар из корзины.  
`clear(): void` - очищает корзину.  
`getTotal(): number` - возвращает общую стоимость товаров в корзине (сумма цен).  
`getCount(): number` - возвращает количество товаров в корзине.  
`hasItem(id: string): boolean` - проверяет наличие товара в корзине по id, возвращает true/false.

### Класс Buyer
Отвечает за хранение и валидацию данных покупателя.

**Конструктор:**  
Не принимает параметров.

**Поля класса:**  
`payment: TPayment | null` - вид оплаты (null, если не указан).  
`email: string` - email покупателя.  
`phone: string` - телефон покупателя.  
`address: string` - адрес доставки.

**Методы класса:**  
`setPayment(payment: TPayment): void` - сохраняет вид оплаты.  
`setEmail(email: string): void` - сохраняет email.  
`setPhone(phone: string): void` - сохраняет телефон.  
`setAddress(address: string): void` - сохраняет адрес.  
`getData(): IBuyer` - возвращает объект с данными покупателя.  
`clear(): void` - очищает все данные покупателя.  
`validate(): Record<string, string>` - валидирует данные и возвращает объект с ошибками. Ключи объекта соответствуют полям (payment, email, phone, address), значения - текст ошибки. Если поле валидно, оно отсутствует в объекте. Поле считается валидным, если оно не пустое.

## Слой представления (View)

Слой представления содержит классы, отвечающие за отображение данных на странице. Каждый класс наследуется от базового класса `Component` и работает с определенным участком DOM.

### Базовый класс CardView

Базовый класс для всех типов карточек товара. Содержит общий функционал для отображения информации о товаре: изображение, название, категория, цена.

**Конструктор:**  
`constructor(container: HTMLElement, events: IEvents)` - принимает DOM-элемент карточки и брокер событий.

**Поля класса:**  
`_title: HTMLElement` - элемент для отображения названия товара.  
`_image: HTMLImageElement` - элемент изображения товара.  
`_category: HTMLElement` - элемент для отображения категории.  
`_price: HTMLElement` - элемент для отображения цены.  
`_product: IProduct | null` - текущий товар карточки.

**Методы класса:**  
`get product(): IProduct` - получить текущий товар.  
`set product(product: IProduct): void` - установить товар и обновить его представление.  
`render(data?: Partial<ICardData>): HTMLElement` - отрендерить карточку.

### Класс CatalogCardView

Карточка товара в каталоге (главная страница). Наследуется от `CardView`. Содержит кнопку "В корзину" и обрабатывает события выбора товара.

**Дополнительные поля:**  
`_button: HTMLButtonElement` - кнопка добавления в корзину.

**Дополнительные методы:**  
`set inBasket(value: boolean): void` - установить состояние кнопки (в корзине или нет).  
`render(data?: Partial<ICardData & { inBasket: boolean; disabled: boolean }>): HTMLElement` - отрендерить карточку каталога с состоянием кнопки.

**События:**  
- `product:select` - при клике на карточку (кроме кнопки).
- `product:add-to-basket` - при клике на кнопку "В корзину".

### Класс BasketCardView

Карточка товара в корзине. Наследуется от `CardView`. Содержит номер позиции и кнопку удаления.

**Дополнительные поля:**  
`_index: HTMLElement` - элемент для отображения номера позиции.  
`_deleteButton: HTMLButtonElement` - кнопка удаления из корзины.

**Дополнительные методы:**  
`set index(value: number): void` - установить номер позиции в корзине.  
`render(data?: Partial<ICardData & { index: number }>): HTMLElement` - отрендерить карточку корзины.

**События:**  
- `product:remove-from-basket` - при клике на кнопку удаления.

### Класс PreviewCardView

Карточка товара в модальном окне предпросмотра. Наследуется от `CardView`. Содержит полное описание товара и кнопку добавления в корзину.

**Дополнительные поля:**  
`_description: HTMLElement` - элемент для отображения полного описания.  
`_button: HTMLButtonElement` - кнопка добавления в корзину.

**Дополнительные методы:**  
`set inBasket(value: boolean): void` - установить состояние кнопки.  
`render(data?: Partial<ICardData & { inBasket: boolean; disabled: boolean }>): HTMLElement` - отрендерить превью.

**События:**  
- `product:add-to-basket` - при клике на кнопку "В корзину".
- `modal:close` - закрытие модального окна после действия.

### Базовый класс FormView

Базовый класс для форм оформления заказа. Содержит общий функционал валидации и управления состоянием кнопки отправки.

**Поля класса:**  
`_submit: HTMLButtonElement` - кнопка отправки формы.  
`_errors: HTMLElement` - элемент для отображения ошибок.

**Методы класса:**  
`set valid(value: boolean): void` - установить состояние кнопки отправки.  
`set errors(value: string[]): void` - установить ошибки валидации.  
`render(data?: Partial<IFormData>): HTMLElement` - отрендерить форму.

**События:**  
- `form:submit` - при отправке формы.

### Класс OrderFormView

Форма первого шага оформления заказа (выбор способа оплаты и адреса). Наследуется от `FormView`.

**Поля класса:**  
`_paymentButtons: HTMLButtonElement[]` - кнопки выбора способа оплаты.  
`_address: HTMLInputElement` - поле ввода адреса.

**События:**  
- `order:payment-change` - при изменении способа оплаты.
- `order:address-change` - при изменении адреса.

### Класс ContactsFormView

Форма второго шага оформления заказа (ввод email и телефона). Наследуется от `FormView`.

**Поля класса:**  
`_email: HTMLInputElement` - поле ввода email.  
`_phone: HTMLInputElement` - поле ввода телефона.

**События:**  
- `contacts:email-change` - при изменении email.
- `contacts:phone-change` - при изменении телефона.

### Класс HeaderView

Заголовок страницы с логотипом, названием и кнопкой открытия корзины.

**Поля класса:**  
`_counter: HTMLElement` - элемент счетчика товаров в корзине.  
`_basketButton: HTMLButtonElement` - кнопка открытия корзины.

**Методы класса:**  
`set counter(value: number): void` - установить значение счетчика.  
`render(data?: Partial<IHeaderData>): HTMLElement` - отрендерить заголовок.

**События:**  
- `basket:open` - при клике на кнопку открытия корзины.

### Класс GalleryView

Галерея товаров на главной странице.

**Методы класса:**  
`set items(items: HTMLElement[]): void` - установить список карточек.  
`render(data?: Partial<IGalleryData>): HTMLElement` - отрендерить галерею.

### Класс BasketView

Модальное окно корзины с списком товаров и кнопкой оформления заказа.

**Поля класса:**  
`_list: HTMLElement` - контейнер списка товаров.  
`_total: HTMLElement` - элемент отображения общей суммы.  
`_button: HTMLButtonElement` - кнопка "Оформить".

**Методы класса:**  
`set items(items: HTMLElement[]): void` - установить список товаров.  
`set total(value: number): void` - установить общую сумму.  
`render(data?: Partial<IBasketData>): HTMLElement` - отрендерить корзину.

**События:**  
- `order:start` - при клике на кнопку оформления заказа.

### Класс ModalView

Модальное окно для отображения содержимого (карточка товара, корзина, форма, экран успеха).

**Поля класса:**  
`_closeButton: HTMLButtonElement` - кнопка закрытия модального окна.  
`_content: HTMLElement` - контейнер для содержимого.

**Методы класса:**  
`set isActive(value: boolean): void` - установить видимость модального окна.  
`set content(value: HTMLElement): void` - установить содержимое модального окна.  
`render(data?: Partial<IModalData>): HTMLElement` - отрендерить модальное окно.

**События:**  
- `modal:close` - при клике на кнопку закрытия или вне модального окна.

### Класс OrderSuccessView

Экран подтверждения успешного оформления заказа.

**Поля класса:**  
`_description: HTMLElement` - элемент описания (сумма заказа).  
`_button: HTMLButtonElement` - кнопка закрытия.

**Методы класса:**  
`set total(value: number): void` - установить сумму заказа.  
`render(data?: Partial<IOrderSuccessData>): HTMLElement` - отрендерить экран успеха.

**События:**  
- `order:success-close` - при клике на кнопку закрытия.

## Типы данных

Все типы данных определены в файле `src/types/index.ts`:

- `TPayment` - тип для способа оплаты ('online' | 'cash').
- `TCategory` - тип для категории товара.
- `TBuyerErrors` - тип для ошибок валидации данных покупателя.
- `IProduct` - интерфейс товара.
- `IBuyer` - интерфейс данных покупателя.
- `IProductsResponse` - интерфейс ответа сервера со списком товаров.
- `IOrderRequest` - интерфейс запроса на создание заказа.
- `IOrderResponse` - интерфейс ответа сервера после создания заказа.
- `IApi` - интерфейс для работы с API.

## События приложения

Приложение использует следующие события для взаимодействия между компонентами:

### События моделей данных:
- `catalog:products-set` - каталог товаров загружен.
- `catalog:preview-set` - товар выбран для просмотра.
- `basket:item-added` - товар добавлен в корзину.
- `basket:item-removed` - товар удален из корзины.
- `basket:change` - содержимое корзины изменилось.
- `basket:cleared` - корзина очищена.
- `buyer:payment-change` - способ оплаты изменен.
- `buyer:address-change` - адрес доставки изменен.
- `buyer:email-change` - email покупателя изменен.
- `buyer:phone-change` - телефон покупателя изменен.
- `buyer:cleared` - данные покупателя очищены.

### События представлений:
- `product:select` - товар выбран для просмотра.
- `product:add-to-basket` - товар добавлен в корзину.
- `product:remove-from-basket` - товар удален из корзины.
- `basket:open` - корзина открыта.
- `order:start` - начало оформления заказа.
- `order:payment-change` - способ оплаты изменен пользователем.
- `order:address-change` - адрес доставки введен пользователем.
- `contacts:email-change` - email введен пользователем.
- `contacts:phone-change` - телефон введен пользователем.
- `form:submit` - форма отправлена.
- `modal:close` - модальное окно закрыто.
- `order:success-close` - экран успеха закрыт.
- `button:click` - кнопка нажата.

## Слой коммуникации

### Класс WebLarekApi
Отвечает за взаимодействие с сервером API "веб-ларёк". Использует композицию с классом Api для выполнения HTTP-запросов.

**Конструктор:**  
`constructor(api: IApi)` - принимает экземпляр класса, реализующего интерфейс IApi.

**Поля класса:**  
`api: IApi` - экземпляр API для выполнения запросов.

**Методы класса:**  
`getProducts(): Promise<IProductsResponse>` - выполняет GET-запрос на эндпоинт /product/ и возвращает объект с общим количеством товаров и массивом товаров.  
`createOrder(order: IOrderRequest): Promise<IOrderResponse>` - выполняет POST-запрос на эндпоинт /order/ с данными заказа и возвращает объект с ID заказа и общей суммой.

